listener 1883 0.0.0.0
allow_anonymous true
persistence false
# The pinned MVP replay has 101,644 observations. Keep seek bursts bounded in memory while allowing
# that complete source to drain to a temporarily slower consumer without broker-side message loss.
max_queued_messages 110000
max_queued_bytes 134217728
log_dest stdout
log_type warning
